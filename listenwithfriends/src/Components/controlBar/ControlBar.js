import React from 'react'
import {ControlBox} from '../Styled/Styled'

function ControlBar() {
    return (
        <ControlBox>
            <button>back</button>
            <button>Play</button>
            <button>Next</button>
        </ControlBox>
    )
}

export default ControlBar
